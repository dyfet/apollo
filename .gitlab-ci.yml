stages:
  - test

merge:
  image: alpine
  stage: test
  script:
    - apk add make musl-dev go
    - go test ./... | tee test.log
  only:
    - merge_requests
  artifacts:
    expire_in: 1 week
    when: always
    paths:
      - ./*.log

